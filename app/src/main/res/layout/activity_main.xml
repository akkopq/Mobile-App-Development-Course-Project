<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/defaultbackground"
    tools:context=".MainActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#ede9fe">

        <!-- Left ImageView (Clickable) -->
        <ImageView
            android:id="@+id/imageViewLeft"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:contentDescription="Logo Icon"
            android:scaleType="fitStart"
            android:src="@drawable/amigo" />

        <!-- Title TextView -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Home"
            android:textColor="#000000"
            android:textSize="18sp" />

        <!-- Right ImageView (Clickable) -->
        <ImageView
            android:id="@+id/imageViewRight"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:contentDescription="Menu Icon"
            android:scaleType="fitEnd"
            android:scaleX="1"
            android:scaleY="0.7"
            android:src="@drawable/bars" />

    </androidx.appcompat.widget.Toolbar>

    <!-- Promotional Content -->
    <!-- Image loaded from a web link -->
    <TextView
        android:id="@+id/promoTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbar"
        android:layout_marginTop="118dp"
        android:gravity="center"
        android:padding="32dp"
        android:text="Bucură-te de fiecare mușcătură la Amigo Fast Food! Descoperă aromele autentice și savuroase.🍔🍟 #DragosteLaFiecareMușcătură"
        android:textColor="#FDFDFD"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/webImageView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="2dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:layout_gravity="center"
        app:srcCompat="@drawable/placeholder" />

</RelativeLayout>
